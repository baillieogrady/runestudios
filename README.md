# Runestudios Theme

Starter WordPress theme using Vite and Tailwind CSS.

## Installation
npm i

## Development
npm run dev

add ```dev=1``` to the url in the browser to load in dev assets

## Build
npm run build

remove ```dev=1``` from the url in the browser to load in build assets
